export type Transection = {
  category: string, 
  date: string, 
  description: string, 
  amount:  number, 
  currency: string, 
  color: string 
}

export type Categories = {
  name: string,
  color: string
}